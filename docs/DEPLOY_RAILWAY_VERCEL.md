# 🚀 คู่มือการ Deploy - Railway + Vercel

## ภาพรวม

คู่มือนี้จะช่วยคุณนำระบบ Dormitory Management System ขึ้นใช้งานจริงโดยใช้:
- **Railway** - สำหรับ Backend, Database (PostgreSQL), และ Redis
- **Vercel** - สำหรับ Frontend (Next.js)

**ค่าใช้จ่าย:** ฟรี (ด้วยเครดิต $5/เดือน ของ Railway และ Free Tier ของ Vercel)

---

## สิ่งที่ต้องเตรียม

- บัญชี GitHub
- บัญชี Railway (สมัครที่ railway.app)
- บัญชี Vercel (สมัครที่ vercel.com)
- บัญชี LINE Developer (สำหรับเชื่อมต่อ LINE)

---

## ส่วนที่ 1: นำ Backend ขึ้น Railway

### ขั้นตอนที่ 1: สร้าง Project ใน Railway

1. ไปที่ [railway.app](https://railway.app)
2. คลิก "Start a New Project"
3. เลือก "Deploy from GitHub repo"
4. เลือก repository `dormitory` ของคุณ
5. เลือกโฟลเดอร์ `backend` เป็น root directory

### ขั้นตอนที่ 2: เพิ่ม Database (PostgreSQL)

1. ในหน้า Project ของ Railway คลิกปุ่ม "+ New"
2. เลือก "Database" → "PostgreSQL"
3. รอระบบสร้าง database สักครู่
4. ไปที่แท็บ "Connect" แล้วก๊อปปี้ค่า `DATABASE_URL` เก็บไว้

### ขั้นตอนที่ 3: เพิ่ม Redis

1. คลิก "+ New" อีกครั้ง
2. เลือก "Database" → "Redis"
3. รอระบบสร้าง Redis สักครู่
4. ไปที่แท็บ "Connect" แล้วก๊อปปี้ค่า `REDIS_URL` เก็บไว้

### ขั้นตอนที่ 4: ตั้งค่า Environment Variables

ในหน้า Railway ของ Backend Service ไปที่แท็บ **Variables** แล้วเพิ่มค่าดังนี้:

```env
# Database (Railway สร้างให้อัตโนมัติ แต่ต้องตรวจสอบ)
DATABASE_URL=postgresql://... (ให้ใช้ Connection String ของ Pooler Port 6543)
DIRECT_URL=postgresql://... (ให้เพิ่มอันนี้! ใช้ Host เดียวกันกับข้างบน แต่เปลี่ยน Port เป็น 5432)

# Redis (Railway สร้างให้อัตโนมัติ แต่ต้องตรวจสอบ)
REDIS_URL=redis://... (ต้องเป็น URL จาก Redis Service ของ Railway ห้ามใช้ localhost)

# Allowed Origins (สำคัญ! ต้องใส่ URL ของ Frontend ที่ไม่มี / ปิดท้าย)
ALLOWED_ORIGINS=https://dormitory-management-frontend.vercel.app

# JWT Secrets (ตั้งรหัสผ่านยาวๆ อะไรก็ได้)
JWT_SECRET=ใส่รหัสลับยาวๆอย่างน้อย32ตัวอักษร
JWT_REFRESH_SECRET=ใส่รหัสลับสำหรับrefreshยาวๆอย่างน้อย32ตัวอักษร

# Node Environment
NODE_ENV=production
PORT=3001

# LINE Integration (เอามาจาก LINE Developers Console)
LINE_CHANNEL_ID=your-line-channel-id
LINE_CHANNEL_SECRET=your-line-channel-secret
LINE_ACCESS_TOKEN=your-line-access-token

# Supabase (ถ้าใช้)
SUPABASE_URL=your-supabase-url
SUPABASE_ANON_KEY=your-supabase-anon-key

# CORS (ใส่ URL ของ Frontend)
CORS_ORIGIN=https://dormitory-management-frontend.vercel.app
```

> **หมายเหตุเรื่อง Database Connection:**
> หากใช้ Supabase ต้องระวังเรื่อง Connection Pooling
> - `DATABASE_URL`: ใช้ Port 6543 (Pooler) สำหรับการใช้งานทั่วไปที่รวดเร็ว
> - `DIRECT_URL`: ใช้ Port 5432 (Session) สำหรับการทำ Migration (Prisma ต้องการอันนี้)

### ขั้นตอนที่ 5: ตั้งค่า Build & Start

ในหน้า Settings ของ Backend Service:

**Root Directory:** `backend`

**Build Command:**
```bash
npm install && npm run prisma:generate && npm run build
```

**Start Command:**
```bash
npm run prisma:migrate:deploy && npm start
```

### ขั้นตอนที่ 6: Deploy

1. คลิกปุ่ม "Deploy"
2. รอจนกว่าจะขึ้นสีเขียว (Success)
3. ก๊อปปี้ URL ของ Backend มา (เช่น `https://dormitory-production.up.railway.app`)

### ขั้นตอนที่ 7: ทดสอบ Backend

ลองเข้า URL ผ่าน Browser หรือ Postman:
```
https://your-backend-url.railway.app/api/health
```

ควรจะได้ผลลัพธ์:
```json
{
  "status": "ok",
  "checks": {
    "database": "healthy",
    "redis": "healthy"
  }
}
```

---

## ส่วนที่ 2: นำ Frontend ขึ้น Vercel

### ขั้นตอนที่ 1: เชื่อมต่อ GitHub กับ Vercel

1. ไปที่ [vercel.com](https://vercel.com)
2. คลิก "Add New Project"
3. เลือก repository `dormitory`
4. เลือกโฟลเดอร์ `frontend` เป็น root directory

### ขั้นตอนที่ 2: ตั้งค่า Build

**Framework Preset:** Next.js
**Root Directory:** `frontend`
**Build Command:** `npm run build`
**Output Directory:** `.next`

### ขั้นตอนที่ 3: ตั้งค่า Environment Variables

เพิ่มค่าเหล่านี้ใน Vercel:

```env
# Backend API URL (เอามาจาก Railway ต้องมี https:// นำหน้า)
NEXT_PUBLIC_API_URL=https://your-backend-url.railway.app

# Node Environment
NODE_ENV=production
```

### ขั้นตอนที่ 4: Deploy

1. คลิก "Deploy"
2. รอจนเสร็จ
3. คุณจะได้ URL ของหน้าเว็บ (เช่น `https://dormitory-frontend.vercel.app`)

---

## ส่วนที่ 3: ตั้งค่า LINE Webhook

1. ไปที่ [LINE Developers Console](https://developers.line.biz/)
2. เลือก Channel ของคุณ
3. ไปที่แท็บ "Messaging API"
4. ตรง Webhook URL ให้ใส่ URL ของ Backend Backway:
   ```
   https://your-backend-url.railway.app/api/line/webhook
   ```
5. เปิดใช้งาน "Use webhook"
6. กด "Verify" เพื่อทดสอบ

---

## การแก้ปัญหาที่พบบ่อย (Troubleshooting)

### 1. Backend สตาร์ทไม่ติด (Crash Loop)
- **เช็ค Logs:** ดูใน Railway ว่า Error อะไร
- **เช็ค Database:** ตรวจสอบว่า `DIRECT_URL` ถูกต้องและเชื่อมต่อได้ (Port 5432)
- **เช็ค Redis:** ตรวจสอบว่า `REDIS_URL` เป็นของจริง ไม่ใช่ localhost

### 2. P1001: Can't reach database server
- เกิดจาก Railway หา Supabase `db.xxxx.supabase.co` ไม่เจอ
- **วิธีแก้:** ให้เปลี่ยน `DIRECT_URL` ไปใช้ Host เดียวกับ Pooler (`aws-0...pooler.supabase.com`) แต่ใช้ Port **5432**

### 3. Frontend เรียก API ไม่ได้ (CORS Error)
- **เช็ค:** ใน Railway ตัวแปร `ALLOWED_ORIGINS` ต้องตรงกับ URL ของ Frontend (ห้ามมี / ปิดท้าย)
- **เช็ค:** ใน Vercel ตัวแปร `NEXT_PUBLIC_API_URL` ต้องมี `https://` นำหน้า

### 4. เรียก API แล้ว Timeout (30s)
- มักเกิดจาก Redis เชื่อมต่อไม่ได้
- **วิธีแก้:** ตรวจสอบโค้ด `queue.ts` ว่ารองรับ `REDIS_URL` หรือไม่ (ซึ่งเราแก้ให้แล้ว) และเช็คค่า `REDIS_URL` ใน Railway

---

## การ Scaling & Backup

- **Railway:** มีแพ็คเกจ Pro ($5/เดือน) ถ้าต้องการ Resource เพิ่ม
- **Database Backup:** Railway มี Auto Backup หรือใช้คำสั่ง `pg_dump` เพื่อ Backup เองได้
